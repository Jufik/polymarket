"""Research sandbox — draft strategies and backtesting harness.

This package imports from polymarket_pipeline but is NEVER imported by it.
The import boundary is enforced by convention and CI checks.
"""
